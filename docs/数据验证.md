# 官方数据 ETL 实测与验收记录

此报告由已保存的真实请求manifest生成；核对发生在本地，不会重新取数或部署。

证据核对时间：2026-09-14T13:11:22.377215Z。语义测试 48 项，失败 0，错误 0，跳过 0。

不同source在各自时间抓取，数量不能相加为美国灾害总数。NWS包括消息版本，NHC一个storm对应多个图层，USGS为全球feed，NWPS为区域/测站样本。

## NWS

实际请求窗口：2026-09-14T12:56:17.576340Z 至 2026-09-14T12:56:39.801001Z；状态 **success**；归一化记录 **35**。

核对 160 份原始响应哈希，16.06 MB；相同规范化输入再次载入产生 0 个新增修订。此幂等测试不代表事件已完成建筑匹配或已获得自动承保授权。

几何角色：warning_polygon=7；forecast_or_county_zone=28。

质量标记：us_scope_requires_property_spatial_match=35；native_geometry_missing=28；coarse_zone_not_damage_footprint=28；fire_weather_is_not_active_wildfire=4；freeze_product_not_building_pipe_temperature=1。

完整证据：`../data/reports/nws_latest.json`；run_id `20260914T125617.576340Z-e9bf62c1`。

## USGS

实际请求窗口：2026-09-14T13:05:36.155176Z 至 2026-09-14T13:05:39.158065Z；状态 **success**；归一化记录 **38**。

核对 9 份原始响应哈希，11.46 MB；相同规范化输入再次载入产生 0 个新增修订。此幂等测试不代表事件已完成建筑匹配或已获得自动承保授权。

几何角色：point=38。

质量标记：global_feed_requires_us_or_portfolio_spatial_filter=38；epicenter_is_not_damage_footprint=38；preliminary_earthquake_parameters=9；shakemap_is_modelled_shaking_not_damage=4。

完整证据：`../data/reports/usgs_latest.json`；run_id `20260914T130536.155176Z-b02ad944`。

## NIFC

实际请求窗口：2026-09-14T12:58:34.380949Z 至 2026-09-14T12:58:48.390674Z；状态 **success**；归一化记录 **446**。

核对 19 份原始响应哈希，37.63 MB；相同规范化输入再次载入产生 0 个新增修订。此幂等测试不代表事件已完成建筑匹配或已获得自动承保授权。

几何角色：observed_perimeter=180；point=266。

质量标记：stale_perimeter=156；observed_perimeter_not_spread_forecast=180；fully_contained_does_not_mean_extinguished=93；perimeter_unavailable=266；incident_point_is_not_fire_extent=266；containment_unknown=249；perimeter_observation_time_unknown=18；stale_incident=317。

完整证据：`../data/reports/nifc_latest.json`；run_id `20260914T125834.380949Z-51a5f715`。

## NHC

实际请求窗口：2026-09-14T12:58:08.787778Z 至 2026-09-14T12:58:09.648427Z；状态 **partial**；归一化记录 **14**。

核对 9 份原始响应哈希，0.07 MB；相同规范化输入再次载入产生 0 个新增修订。此幂等测试不代表事件已完成建筑匹配或已获得自动承保授权。

几何角色：point=2；forecast_cone=2；forecast_track=4；observed_wind_extent=1；forecast_wind_extent=5。

质量标记：us_boundary_spatial_match_pending=14；storm_center_is_not_impact_footprint=14；cone_is_not_wind_footprint=2；track_is_not_impact_footprint=4；analyzed_extent_not_ground_observation=1；forecast_valid_time_unavailable=5。

完整证据：`../data/reports/nhc_latest.json`；run_id `20260914T125808.787778Z-53a122ab`。

## NWPS

实际请求窗口：2026-09-14T13:05:39.202279Z 至 2026-09-14T13:06:00.206188Z；状态 **success**；归一化记录 **22**。

核对 1 份原始响应哈希，2.85 MB；相同规范化输入再次载入产生 0 个新增修订。此幂等测试不代表事件已完成建筑匹配或已获得自动承保授权。

几何角色：point=22。

质量标记：gauge_point_is_not_inundation_footprint=22；property_reach_relationship_required=22；forecast_issue_time_unavailable_in_summary=14；collection_scope_is_sample_not_national=22；action_stage_does_not_mean_flooding=18。

完整证据：`../data/reports/nwps_latest.json`；run_id `20260914T130539.202279Z-b150939d`。

## 如何解读结果

- NWS：原生warning polygon与zone fallback分别标记；zone是较粗的预警行政/预报范围，不是逐栋损伤范围。实际发现批量zones接口返回空几何，使用单zone备用路径补齐。
- NIFC：仅point的火灾仍是已知事件，范围未知。100% containment不自动等于扑灭；旧边界不能由于本次成功下载而刷新观测年龄。全量几何抓取已验证，但生产应改增量。
- NHC：一个热带低压初始风圈为零面积，已隔离；因此此source整体为partial，可用记录在normalized/revisions，不发布为完整current。预测风圈缺逐面有效时间，不能作为自动moratorium时间范围。
- USGS：38条等计数来自全球M2.5+ past-day；4份ShakeMap grid解析成功。震中二维，深度单独为km；MMI/PGA是震动估计，不是房损模型。
- NWPS：全国接口曾超时；东南部2,710 gauges的独立probe成功，另一次统一runner的同区域请求也曾超时，反映延迟/可用性限制。应采用portfolio测站或更小区域切片。以该source最新manifest确认本次统一run的具体scope；不能据sample成功声称全国成功。
- 本次初始网络沙箱拒绝公开socket，获自动审核允许后已执行公开读取；失败run仍保留历史。没有账户授权信息、没有部署。

## 尚未证明

全国portfolio空间匹配、真实建筑精度、全年高峰可用性、无漏报、受影响财产数量、费用/月度SLA、rating效果和moratorium业务正确性均未由本次样本证明。生产拓扑与日期变更线、源历史回补及多scope发布需要下一阶段实现。
